# Core DL
torch>=2.0
torchvision>=0.15

# Geospatial
rasterio>=1.3
# gdal is best installed via conda on local; on Kaggle it is preinstalled

# Imaging / arrays
numpy>=1.24
opencv-python>=4.8
scikit-image>=0.21
albumentations>=1.3
Pillow>=10.0

# Metrics / utils
scipy>=1.10
tqdm>=4.65
matplotlib>=3.7

# Demo
gradio>=4.0

# Data acquisition (Google Earth Engine). Only needed to run scripts/gee_export.py;
# preinstalled on Kaggle/Colab, so `pip install earthengine-api geemap` on demand.
earthengine-api>=0.1.380
geemap>=0.30
